name: Test Paths Conditional

on:
  push:
    branches:
      - main
    paths:
      - '*.py'
      - 'config.yml'
      - 'data/**'

jobs:
  Test:
    name: Echo Paths Trigger
    runs-on: ubuntu-latest
    steps:
      - name: Display trigger event
        run: |
          echo "Trigger Event: ${{ github.event_name }}"
          echo "Elements in Paths that changed: ${{ github.event.head_commit.modified }}"
          echo "Event Details: ${{ toJson(github.event) }}"
